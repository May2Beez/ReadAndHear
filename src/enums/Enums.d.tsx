export enum DarkMode {
	Light = "light",
	Dark = "dark",
}

export enum PageSliding {
	LeftRight = "leftRight",
	UpDown = "upDown",
}

export enum SinglePage {
	Single = "single",
	Multi = "multi",
}
